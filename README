Basic Flask application allowing the user to upload photos onto a database using an http link and giving the image a name.
Able to search all existing images in the database by typing the name of the image.